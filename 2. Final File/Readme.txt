This is the final model which is developed. It is used for detecting handwritten capital letters with the accuracy of 71%, which is the max possible value with 
Naive Bayes Classifier.

The previous issue was resolved, we were getting max values as 255, because we were drawing images on white background with black marker. But the data was trained with images 
on black background with white marker.
